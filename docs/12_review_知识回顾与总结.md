# 知识回顾与总结（12_review）

> 目标：沉淀阶段性复盘，减少重复踩坑，便于后续迭代快速接手。

## 1. 本期回顾范围

- 时间范围：2026-05-14（MVP W1-W8）
- 回顾对象：模板管理、对话填充、导入填充、AI 提取、文档生成
- 输出类型：经验总结 + 风险清单 + 后续行动项

## 2. 关键结论（可持续更新）

- 架构分层清晰后，功能迭代速度显著提升。
- 导入链路通过“规则 + AI 回退”策略提高了可用性。
- 对话链路中 SSE 对体验提升明显，但需特别关注异常中断处理。
- 文档生成闭环稳定后，模块重点从“能生成”转向“生成质量与验收效率”。

## 3. 经验沉淀

### 3.1 做得好的

- 规范化文档命名（`NN_类型_主题.md`）提升了索引与协作效率。
- 验收文档与实现流程分离，便于追溯。
- API 与架构文档同步维护，减少前后端理解偏差。

### 3.2 可改进的

- 统一错误码仍需在代码返回层全面落地。
- 部分文档示例与真实响应结构仍存在“建议态”与“实现态”差异。
- 自动化验收脚本覆盖不足，仍依赖手工回归。

## 4. 常见问题复盘

- 问题：文件存储路径在不同运行目录下漂移。
  - 处理：统一使用 `ContentRootPath` 作为相对路径锚点。
- 问题：流式接口在代理层可能被缓冲。
  - 处理：SSE 响应头关闭缓冲并做前端超时提示。
- 问题：AI 提取结果偶发为空。
  - 处理：回退规则解析并提示用户人工确认。

## 5. 后续行动项（Next Actions）

1. 在后端统一落地错误码响应结构（`code/message/details/requestId`）。
2. 增补 API 验收自动化脚本，覆盖核心用例 `API-HEALTH` 到 `API-IMP`。
3. 对低置信度字段增加批量确认与风险提示策略。
4. 完成 W8 验收收尾材料并固化发布清单。

## 6. 复盘模板（复制即用）

```text
回顾周期：
目标达成情况：
本期关键成果：
本期主要问题：
问题根因：
修复与缓解措施：
沉淀结论：
下期行动项：
负责人/截止时间：
```
